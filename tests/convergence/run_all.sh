#!/bin/bash

# One script to rule them all.
# A single bash script to run all convergence tests. If you'd instead like to
# run specific convergence tests, run the bash script corresponding to that
# test, e.g., test_mhdmodes1d.sh

./test_mhdmodes1d.sh
./test_mhdmodes2d.sh